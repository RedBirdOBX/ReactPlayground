import React from 'react';

const FooComponent = (props) =>
{
   return (
       <div>
           This is the Foo component.
           This is rendered when the url is "/foo".
        </div>
   );
};

export default FooComponent;