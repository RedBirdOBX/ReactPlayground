import React from 'react';

const Book = (props) =>
{
    return (
        // <div className="answer" onClick={() => { props.BookOnClick(props.Title); }}>
        //     {props.Title}
        // </div >
        <div>turn</div>
    );
};

export default Book;