import React from 'react';

class HeaderComponent extends React.Component {
    render() {
        return (<p className="lead">Click the buttons to update the state's Counter value.</p>);
    }
}

export default HeaderComponent;